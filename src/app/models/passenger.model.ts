import { Ubigeo } from "./ubigeo.model";

export class Passenger {
    idPassenger?: number;
    names?: string;
    lastNames?: string;
    distrit?: Ubigeo;
    address?: string;
    phone?: string;
}
